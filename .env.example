# ============================================================================
# ListPull Configuration File
# ============================================================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to 'listpull.env' in the same directory
# 2. Fill in ALL required fields marked with [REQUIRED]
# 3. Run: sudo ./deploy/install.sh
#
# The installer will validate this file before proceeding.
# ============================================================================

# ============================================================================
# SERVER CONFIGURATION [REQUIRED]
# ============================================================================

# JWT Secret - Used to sign authentication tokens
# [REQUIRED] Generate with: openssl rand -hex 32
# Must be at least 32 characters
JWT_SECRET=

# Server port (usually leave as 3000)
PORT=3000

# Database path (usually leave as default)
DATABASE_PATH=./data/listpull.db

# JWT token expiry (e.g., 7d, 24h, 1w)
JWT_EXPIRY=7d

# ============================================================================
# STORE INFORMATION [REQUIRED]
# ============================================================================

# Your store's name - displayed throughout the app
# [REQUIRED]
VITE_STORE_NAME=

# Contact email for customers
# [REQUIRED]
VITE_STORE_EMAIL=

# Store phone number
# [REQUIRED] Format: XXX.XXX.XXXX (will display as (XXX) XXX-XXXX)
VITE_STORE_PHONE=

# Store address
# [REQUIRED]
VITE_STORE_ADDRESS=

# These are duplicated for the backend (used in email templates)
STORE_NAME=
STORE_EMAIL=
STORE_PHONE=
STORE_ADDRESS=

# ============================================================================
# ORDER CONFIGURATION
# ============================================================================

# Prefix for order numbers (e.g., LP-240215-AB12)
VITE_ORDER_PREFIX=LP
ORDER_PREFIX=LP

# Number of days orders are held before being returned to inventory
VITE_ORDER_HOLD_DAYS=7

# ============================================================================
# EMAIL CONFIGURATION [OPTIONAL]
# ============================================================================
# Leave SMTP_HOST empty to disable email notifications.
# Customers can still check order status on the website.
#
# --- OPTION 1: Gmail with App Password ---
# 1. Enable 2-Factor Authentication on your Google account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate an app password for "Mail"
# 4. Use that 16-character password below (no spaces)
#
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=youremail@gmail.com
# SMTP_PASS=xxxx xxxx xxxx xxxx  (your app password, spaces optional)
# SMTP_SECURE=false
# FROM_EMAIL=youremail@gmail.com
# FROM_NAME=Your Store Name
#
# --- OPTION 2: Other SMTP Provider ---
# Configure with your provider's settings
#
# ============================================================================

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
FROM_EMAIL=
FROM_NAME=

# ============================================================================
# ADVANCED SETTINGS (usually no need to change)
# ============================================================================

# Maximum file size for decklist uploads in MB
VITE_MAX_FILE_SIZE_MB=1

# Maximum number of cards allowed in a single decklist
VITE_MAX_DECKLIST_CARDS=500

# API rate limiting (milliseconds between requests)
VITE_SCRYFALL_RATE_LIMIT_MS=100
VITE_POKEMON_RATE_LIMIT_MS=200
VITE_AUTOCOMPLETE_DEBOUNCE_MS=200

# API base URL (leave as /api for same-origin)
VITE_API_URL=/api
